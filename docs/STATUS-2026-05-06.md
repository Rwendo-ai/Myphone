# Status snapshot — 2026-05-06

End-of-session handoff for the multi-day push that started with the
streaming pivot (2026-05-05) and ended with the 5-language expansion
(tonight). This doc captures **what shipped, what broke, what's queued**,
and **what the next EAS rebuild MUST include**.

> Read this first when starting the next session. Supersedes
> [STATUS-2026-05-05.md](./STATUS-2026-05-05.md).

---

## 1. TL;DR — what changed across this stretch

### Course content (the big lift)

- **5 new language pairs added: Hindi / Spanish / Portuguese (Brazil) / Japanese / Korean.**
  Each with both directional variants (English speaker → X AND X speaker → English).
- **5 new speaker packs authored** — locale JSONs translated, AI prompts
  in native language, voices wired (using ElevenLabs multilingual v2 IDs).
- **1000 lessons now in Supabase Storage** (up from 576 yesterday). 911 new
  uploads tonight + the 576 from the streaming pivot.
- **Per-course manifests regenerated from Storage** for all 18 courses.

| Forward (English → X) | Reverse (X → English) | Speaker pack |
|---|---|---|
| Hindi 100/100 ✅ | Hindi 100/100 ✅ | hindi ✅ |
| Spanish 99/100 ⚠️ (m04-l10 missing) | Spanish 100/100 ✅ | spanish ✅ |
| Portuguese 100/100 ✅ | Portuguese 95/100 ⚠️ (M10 missing 5) | portuguese ✅ |
| Japanese 26/100 ⚠️ (74 missing) | Japanese 92/100 ⚠️ (M10 missing 8) | japanese ✅ |
| Korean 100/100 ✅ | Korean 100/100 ✅ | korean ✅ |

**88 lessons still missing across the 5 new pairs.** Quota hit on the longer
agent runs overnight.

### App-level changes (the wiring)

- Data registries: `data/speakers/index.ts`, `data/courses/index.ts`,
  `data/languages/index.ts` — all extended for the 5 new languages.
- `lib/i18n.ts` — `SUPPORTED_LANGUAGES` extended from 5 to 10 ISO codes
  (`hi`/`es`/`pt`/`ja`/`ko` added) + 5 new locale JSON imports + new
  `setAppLanguage` mappings.
- `app/(tabs)/profile.tsx` — `ISO_TO_DB` and `LANG_FLAGS` extended for
  10 languages.
- `app/(auth)/onboarding.tsx` — speaker selector `LANGUAGE_IDS` extended
  to 10.
- `lib/SettingsContext.tsx` — `learnedLanguage` / `spokenLanguage` now
  derive from v3 state (speaker + activeCourseId), not the legacy
  `activePack`. Also `setSpeakerPack` auto-promotes `activeCourseId` if
  the previous course isn't available to the new speaker.
- `language-english/index.ts` `availableForSpeakers` → all 9 non-English
  speakers (shona/french/chinese/tagalog/hindi/spanish/portuguese/japanese/korean).
- Each new language course (`language-hindi`/spanish/portuguese/japanese/korean)
  has `availableForSpeakers: ['english']` only — restricted to speakers
  with authored content. Loosen as variants get authored.

### Decision docs created

- [docs/LANGUAGE-SECTION-DESIGN.md](./LANGUAGE-SECTION-DESIGN.md) — locked:
  language course = country bundle (single SKU includes phrasebook,
  cultural guide, airport, safari for the destination). Free travel-tier
  features (FX, weather, translate, meet travellers) are the funnel.
- [docs/curriculum/CHRISTIAN-FOUNDATION-SPEC.md](./curriculum/CHRISTIAN-FOUNDATION-SPEC.md) —
  governance for Build Yourself courses; Buddhist scaffolding ripped out
  of 18 lessons across 7 BY courses (commits via this session).
- Pack-authoring docs ([PACK-AUTHORING-COURSE.md](./PACK-AUTHORING-COURSE.md),
  [SPEAKER.md](./PACK-AUTHORING-SPEAKER.md), [JURISDICTION.md](./PACK-AUTHORING-JURISDICTION.md))
  aligned with streaming-pivot reality.

---

## 2. App-level assessment (NOT course content)

The user asked for a clear-eyed look at what the **app code itself** needs,
separate from authoring. Here's what the audit found:

### What works

- Streaming lesson loader (`lib/lesson-loader.ts`): cache → Storage → English fallback. `installCourse` + `uninstallCourse` functions exist and work.
- 5 speaker packs registered with correct locale JSONs, AI prompts, voice IDs.
- Learn tab + unit screen + lesson screen wired end-to-end through the streaming pipeline.
- Profile screen with collapsible Color Theme + App Language sections + native-name display (English / ChiShona / Français / 中文 / Tagalog / हिन्दी / Español / Português / 日本語 / 한국어).
- `entitlements.ts` exists with `canAccessLesson` / `canAccessCourse` / `canUseAiFeature`. `DEV_UNLOCK_ALL` bypass works.
- Onboarding asks for speaker pack, jurisdiction, learning goal, course choice, etc. PH/FR jurisdictions wired (commit `3272230`).

### What's broken or missing in the app (NOT course content)

#### A. Deferred since 2026-05-05 (must land before EAS rebuild)

| ID | Item | Status | Effort |
|---|---|---|---|
| **A1** | Drop diagnostic `console.log`/`console.warn` in `lib/lesson-loader.ts` (3 lines around cache+Storage paths) and `app/lesson/[id].tsx` (3 lines in load `useEffect`) | Code change | 5 min |
| **A2** | **Lazy-translation Edge Function** (`supabase/functions/translate-lesson/index.ts`) + client wiring in `lib/lesson-loader.ts` | New code | ~3-4 hrs |
| **A3** | **Wire `crisisHandoff` at runtime** — lesson screen reads `lesson.crisisHandoff === true`, skips mission, surfaces jurisdiction's `crisisResources`. Schema field exists, runtime doesn't read it. | Code change | ~1 hr |
| **A4** | **Install / uninstall UI** — per-course pill on Learn tab ("Installed · 480 KB" / "Tap to download") + Profile → Storage panel. All wiring exists in `lib/lesson-loader.ts`. | Pure UI | ~2-3 hrs |
| **A5** | **Cache-version field** — add `version` to lesson JSONs; loader compares cached vs Storage on read; mismatch triggers refetch. We just edited shipped lessons (BY rewrites + new languages) so this is no longer optional. | Code change + script update | ~1-2 hrs |
| **A6** | **Phase 8 graceful exit detection** — keyword match for "stop / I'm done / exit / back" + duration > 30s, save partial progress, award proportional XP. | Code change | ~1 hr |

#### B. New gaps the user surfaced tonight

| ID | Item | Notes | Effort |
|---|---|---|---|
| **B1** | **Build Yourself courses showing only 1 lesson** | Likely cause: device is on a stale binary (manifest in JS bundle is from before regeneration) OR cached lesson list wasn't refreshed. Confirmed all 8 BY courses have correct manifests in repo (KY 10, HardConv 8, Parenting 12, Money 10, Grief 10, Sleep 8, LostConf 8, Caring 10). **Action: include in next EAS rebuild + force cache clear.** | bundled in rebuild |
| **B2** | **Knowing Yourself Units 2-10 unauthored** | Only Unit 1 (10 lessons) in Storage. Plan exists at [KNOWING-YOURSELF-CURRICULUM-PLAN.md](./curriculum/KNOWING-YOURSELF-CURRICULUM-PLAN.md). 90 more lessons needed. | future authoring |
| **B3** | **Daily XP bug** — dev account shows 25 XP that was earned a week ago | `lib/preferences.ts` has the goal preference key but no logic resetting daily XP at midnight. **Action: trace the daily-xp render in Learn tab → it computes `xp % dailyXpGoal` from total XP, which means the daily progress bar is just `total XP mod goal`, not "XP earned today." This is by design but confusing.** Either rename to "progress to next goal" or implement actual today-XP tracking with a `last_reset_at` column. | ~1-2 hrs to redesign |
| **B4** | **Companion system gaps** — preset definitions in `data/companions/presets.ts` but `public.companions` table sync unverified. Companion facts/summaries pipeline exists (`lib/conversation-memory.ts` per past status) but stitching to companion-tab UI may have drifted. | requires audit + Supabase check |
| **B5** | **No payment gateway / paywall UI** — `entitlements.ts` exists for runtime gating; no IAP wiring; "Buy" alert in Learn tab is a placeholder. | Phase H (RevenueCat + App Store + Play Store) |
| **B6** | **No phrasebook / flip cards on Learn tab** | New feature per user request (item 2 in their list). Spec below. | ~ days of work |
| **B7** | **No crypto / xrpl integration** | Brand new workstream (item 8/9). | weeks |

#### C. Stale config / cleanup

| ID | Item | Notes |
|---|---|---|
| **C1** | `EAS development build profile env`: `EXPO_PUBLIC_DEV_UNLOCK_ALL=1` shipped to TestFlight builds — **strip this before public release** (`production` profile already correct). | one-line `eas.json` audit |
| **C2** | `data/lessons.ts` `english-shona` legacy `UNITS` block (1-lesson placeholder) is no longer referenced by Learn tab (fixed yesterday) but still in source. Safe to delete. | ~5 min |
| **C3** | `scripts/originals/` directory — temporary references for agent runs. Should be `.gitignore`d. | ~2 min |
| **C4** | Pre-commit hooks — none. Adding `tsc --noEmit` as pre-commit would have caught the broken Portuguese/Spanish lessons before upload. | ~30 min |

---

## 3. The user's 10-item list — itemised

For each, my read of scope, fit-with-app, and where it fits in the queue.

| # | User's item | Category | When |
|---|---|---|---|
| 1 | Complete lesson rollout v1 | Authoring | 88 missing lessons + KY Units 2-10 = ~178 lessons. **Two 4-hour authoring sessions.** Defer to tomorrow + next session. |
| 2 | Flip cards + phrasebook (move to Learn tab) — 500 words/course with images + ElevenLabs single-word audio | New feature | Big — needs design pass first. **Add to language-section-design.md as a phase 2 item** |
| 3 | "Other jobs from your study" | Various | Captured in §2 above |
| 4 | BY courses only 1 lesson showing | Bug | **Likely fixed by next EAS rebuild** (B1 above) — manifests are correct in repo |
| 5 | Companion prompt/personality/soul + DB sync + facts/summaries stitched | Bug + feature | **B4 above** — needs audit + Supabase check |
| 6 | Daily XP bug (25xp showing a week later) | Bug | **B3 above** — design decision: redesign vs. relabel |
| 7 | XP point system gamification (per-action point rules) | New system | Spec exists in user's message. Needs `xp_events` table + `awardXp(reason, amount)` API. **~1 day to wire, then iterate per action type.** |
| 8 | Crypto integration (xrpl, points → tokens) | New workstream | Big. Architecturally separate. **2-4 weeks of focused work.** |
| 9 | Crypto airdrop + vesting + docs | Same as 8 | Bundled with item 8 |
| 10 | Paywall + payment gateway | New workstream | Phase H of original plan. **RevenueCat + App Store Connect + Play Console + StoreKit testing.** ~1-2 weeks. |

---

## 4. EAS rebuild — what MUST land in the new build

These are code changes. New build won't ship them unless they're committed
(or at least merged to dev branch) before `eas build`.

### Mandatory before rebuild
- [ ] **A1 — drop console.logs** (5 min)
- [ ] **A3 — crisisHandoff runtime wiring** (1 hr)
- [ ] **A4 — install/uninstall UI** (2-3 hrs)
- [ ] **A5 — cache version field + version bump** (1-2 hrs)
- [ ] All 5 new languages (already wired in repo, just needs to be in the bundle)
- [ ] Build Yourself courses fix (manifests correct in repo; just needs bundle refresh)

### High-value but deferrable
- [ ] **A2 — lazy-translation Edge Function** — can ship without it; first-time non-English-speaker access falls back to English variant for now. Add in next build cycle.
- [ ] **A6 — Phase 8 graceful exit** — annoying but not breaking
- [ ] **B3 — Daily XP redesign** — depends on whether we want to fix the spec or the implementation

### Strip before submission
- [ ] **C1 — `DEV_UNLOCK_ALL=1` env on `development` profile is fine for TestFlight, but strip from `production`** (probably already correct — verify)

### Suggested commit boundary for the rebuild
```
1. cleanup: drop diagnostic console.logs (A1)
2. ui: install/uninstall course pill + Profile Storage panel (A4)
3. runtime: wire lesson.crisisHandoff for Grief/Parenting/Caring (A3)
4. cache: add version field + script bump utility (A5)
5. languages: 5 new language pairs (Hindi/Spanish/Portuguese/Japanese/Korean) registered + speaker packs + locale JSONs + i18n + profile + onboarding wiring
```

Single tagged release: `v0.3-streaming-multi-language`.

---

## 5. Today's "do now" job list — quick wins (≤ 60 min each)

For the next session: knock these off in order. Each is independently
shippable; commit after each.

1. **Drop console.logs (A1)** — 3 grep+delete operations across 2 files. 5 min.
2. **Wire crisisHandoff (A3)** — read `lesson.crisisHandoff`, branch the mission render. 30-60 min.
3. **Add version field to lesson JSON + loader compare (A5)** — `LessonData` field + 4 lines in `lib/lesson-loader.ts`. 30-45 min.
4. **Install/uninstall UI on course pill (A4 partial)** — minimal version: per-pill "Installed ·  KB / Uninstall" toggle. Profile Storage panel later. 60-90 min.
5. **Verify BY-course manifest landing on device** — once new build ships, confirm Knowing Yourself shows all 10 lessons (currently 1 unit × 10 lessons, all in Module 1).
6. **`tsc --noEmit` final**, commit batch, `eas build --profile development --platform android` (faster than iOS), test on device.

After this: tackle the bigger items — XP system, lazy translation, KY Units 2-10 authoring.

---

## 6. After-rebuild queue — the bigger items

Roughly priority-ordered. None are quick.

1. **XP system overhaul (item 7)** — `xp_events` table, `awardXp(reason, amount)` API, per-action rule registry, daily XP redesign with proper `last_reset_at`. ~1-2 days.
2. **Lazy-translation Edge Function (A2)** — supabase/functions/translate-lesson, client wiring. ~1 day.
3. **Companion DB sync + facts pipeline audit (B4 / item 5)** — verify `public.companions` matches preset definitions, trace facts pipeline, stitch any gaps. ~1 day.
4. **Knowing Yourself Units 2-10 authoring (item 1 partial)** — 90 lessons. Parallel agents per unit. ~1 session.
5. **Complete remaining 88 language lessons (item 1 partial)** — Spanish 1, Japanese forward 74, Portuguese reverse 5, Japanese reverse 8. Parallel agents. ~half session.
6. **Phrasebook + flip cards on Learn tab (item 2 / B6)** — 500 words/course × 18 courses = 9000 ElevenLabs audio clips. Big infra + new UI surface. ~1-2 weeks.
7. **Phase H — RevenueCat + paywall + IAP (item 10 / B5)** — App Store Connect + Play Console + RevenueCat dashboard + product creation + entitlement mapping + StoreKit testing. ~1-2 weeks once Apple Developer enrollment finalises.
8. **Crypto / xrpl integration (items 8/9 / B7)** — XRPL wallet, vesting, airdrop logic, regulator review. ~3-4 weeks; legal review separately.

---

## 7. Open questions / decisions needed

- **Daily XP design (B3 / item 6)**: redesign as actual "today's XP" with reset-at-midnight, OR rename current display to "progress to goal"? Current implementation is `xp % dailyXpGoal` which is misleading.
- **Phrasebook scope (item 2)**: 500 words per language × 10 languages = 5000 unique target-language words. ElevenLabs audio for each (~$0.05/word at multilingual v2 = ~$250 total, manageable). But authoring effort: who curates the 500 words per language?
- **XP earn rules (item 7)**: user proposed schedule (course = 25, login = 5, text = 5, voice = 15, basic avatar = 50, top avatar = 100). Confirm and lock these; per-action enforcement is the implementation work.
- **Crypto path (items 8/9)**: XRPL airdrop with vesting is regulator-sensitive in Australia. Engage a lawyer before building. Defer until paywall (item 10) is shipping revenue.

---

## 8. Recovery / rollback context

- Recovery git tag `v0.2-pre-streaming-refactor` from yesterday still exists. Pre-streaming-pivot state.
- All commits since are forward — no destructive operations.
- Storage holds 1000 lesson objects; no deletions tonight beyond 3 stale slugs (KY l03 / l09 / Parenting l07 from BY rewrites).
- 5 new speaker packs are additive — removing them doesn't break existing speakers.

If the new build regresses, revert to commit `58a0e4f` and rebuild — that's the pre-streaming-pivot stable state.

---

*End of status. Next session: start with §5 ("do now" list) before tackling §6.*
