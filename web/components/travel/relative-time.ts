// Re-export the home helper so travel components don't need a sibling import.
// Keeps each component dir self-contained without duplicating logic.
export { relativeTime } from '../home/relative-time';
